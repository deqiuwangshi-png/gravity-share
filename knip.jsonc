{
  "$schema": "https://unpkg.com/knip@latest/schema.json",
  // 死代码检测配置：knip 内置 next 插件自动豁免 app/**/page.tsx、layout.tsx、route.ts 等约定文件，
  // 内置 vitest 插件识别 *.test.ts，均无需手动配置。
  // 存量死代码：开发时对 pnpm check 检出的零引用导出/文件当场清理（AGENTS.md「死代码护栏」）。
}
